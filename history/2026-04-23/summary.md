Overnight I ran 57 research iterations against our scoring benchmarks, keeping 2 meaningful improvements and reverting the rest. The net result is a score jump from 0.2857 to 0.5714, doubling our baseline. The first win was a fallback search in product handling that retries with relaxed filters when the initial query comes back empty, which rescues cases where the LLM over-specifies extraction parameters. The second added voucher-aware pricing logic that derives a pre-discount price ceiling from the total budget, lifting our shop score to 0.5. Voucher category scoring is still at zero, so that is where I will focus the next run. If anyone has seen specific voucher query patterns that feel like they should resolve but do not, I would love to hear about them.